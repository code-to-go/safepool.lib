package access

import "testing"

func TestAccess(t *testing.T) {

	/*

		acc = NewAccess(identity, stores)
		acc.AddToGroup(group, id)
		encKey = acc.GetKey(group)

		acc.Upload(group, name, r)
		acc.Download(group, name, w)
		acc.List(group) names[]

	*/
}
